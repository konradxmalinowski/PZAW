# Quiz - Wprowadzenie do Node.js i środowiska pracy

20 pytań jednokrotnego wyboru do samodzielnego sprawdzenia wiedzy z tematu `1-podstawy.md`. Odpowiedzi znajdziesz na końcu pliku - spróbuj najpierw odpowiedzieć samodzielnie.

**1. Czym jest Node.js?**

A. Językiem programowania, alternatywą dla JavaScriptu
B. Środowiskiem uruchomieniowym pozwalającym wykonywać JavaScript poza przeglądarką
C. Frameworkiem do budowania interfejsów użytkownika
D. Bazą danych używaną w aplikacjach webowych

**2. Który silnik JavaScript wykorzystuje Node.js?**

A. SpiderMonkey
B. Chakra
C. V8
D. JavaScriptCore

**3. Którego z poniższych obiektów NIE znajdziesz w Node.js (bo jest dostępny tylko w przeglądarce)?**

A. `process`
B. `document`
C. `require`
D. `console`

**4. Do czego służy moduł `fs` w Node.js?**

A. Do obsługi żądań HTTP
B. Do pracy z systemem plików (odczyt/zapis plików)
C. Do generowania losowych liczb
D. Do zarządzania zależnościami projektu

**5. Co dokładnie wykonuje Event Loop w Node.js?**

A. Kompiluje kod JavaScript do kodu maszynowego
B. Pilnuje zadań oczekujących na wykonanie i uruchamia je, gdy są gotowe
C. Instaluje zależności projektu
D. Tworzy nowe wątki dla każdego żądania HTTP

**6. Jaki będzie wynik poniższego kodu?**

```javascript
console.log("1");

setTimeout(() => {
    console.log("2");
}, 1000);

console.log("3");
```

A. 1, 2, 3
B. 1, 3, 2
C. 3, 1, 2
D. 2, 1, 3

**7. Czym różni się kod asynchroniczny od synchronicznego?**

A. Kod asynchroniczny nie może zwracać żadnej wartości
B. Kod asynchroniczny pozwala rozpocząć operację i nie czekać na jej zakończenie, żeby wykonywać dalszy kod
C. Kod synchroniczny działa tylko w przeglądarce
D. Nie ma żadnej różnicy, to dwie nazwy tego samego mechanizmu

**8. Dlaczego nie należy blokować Event Loop bardzo ciężkimi obliczeniami synchronicznymi?**

A. Bo Node.js automatycznie przerywa takie obliczenia po 5 sekundach
B. Bo przez ten czas Node.js nie może obsługiwać żadnych innych zadań, w tym nowych żądań HTTP
C. Bo taki kod nie skompiluje się w silniku V8
D. Bo zużywa to więcej pamięci RAM niż kod asynchroniczny

**9. Czy Node.js jest wielowątkowy?**

A. Tak, każde żądanie HTTP jest obsługiwane przez osobny wątek JavaScript
B. Nie, kod JavaScript w Node.js jest zasadniczo wykonywany przez pojedynczy główny wątek
C. Tak, liczba wątków zależy od liczby rdzeni procesora
D. Node.js w ogóle nie korzysta z wątków, nawet systemowych

**10. Jakim poleceniem sprawdzisz zainstalowaną wersję Node.js?**

A. `node --check`
B. `npm version`
C. `node --version`
D. `node install`

**11. Co oznacza skrót npm?**

A. New Package Manager
B. Node Package Manager
C. Node Project Model
D. Network Package Method

**12. Które polecenie utworzy plik `package.json` bez zadawania pytań?**

A. `npm init`
B. `npm start`
C. `npm init -y`
D. `npm create`

**13. Które z poniższych NIE jest standardowym polem w pliku `package.json`?**

A. `scripts`
B. `dependencies`
C. `main`
D. `routes`

**14. Czym różnią się `dependencies` od `devDependencies`?**

A. `dependencies` to biblioteki potrzebne do działania aplikacji, `devDependencies` to biblioteki potrzebne głównie podczas tworzenia aplikacji
B. `dependencies` są instalowane automatycznie, a `devDependencies` trzeba instalować ręcznie z internetu
C. To dokładnie to samo pole, zapisane w dwóch różnych konwencjach
D. `devDependencies` dotyczą tylko frontendowych bibliotek

**15. Do czego służy plik `package-lock.json`?**

A. Blokuje możliwość edycji `package.json` przez innych programistów
B. Zapisuje dokładne wersje wszystkich zależności (także pośrednich), żeby zapewnić powtarzalność instalacji
C. Przechowuje hasła i dane logowania do projektu
D. Jest kopią zapasową katalogu `node_modules`

**16. Dlaczego katalogu `node_modules` zwykle NIE dodaje się do repozytorium Git?**

A. Git nie obsługuje katalogów o takiej nazwie
B. Jest bardzo duży, a jego zawartość można odtworzyć poleceniem `npm install` na podstawie `package.json`
C. Zawiera dane wrażliwe, takie jak hasła
D. npm automatycznie usuwa ten katalog przy każdym uruchomieniu

**17. Jak uruchomisz skrypt o nazwie `dev` zdefiniowany w `package.json`?**

A. `npm dev`
B. `node dev`
C. `npm run dev`
D. `npm start dev`

**18. Co zwraca `process.argv` w Node.js?**

A. Listę wszystkich zainstalowanych pakietów
B. Tablicę argumentów przekazanych przy uruchomieniu programu (wraz ze ścieżkami do node i pliku)
C. Zawartość pliku `package.json`
D. Listę zmiennych środowiskowych systemu

**19. Który zapis poprawnie odczytuje zmienną środowiskową `PORT` z wartością domyślną `3000`, jeśli zmienna nie jest ustawiona?**

A. `const port = process.env.PORT || 3000;`
B. `const port = process.PORT.env || 3000;`
C. `const port = env.PORT ?? process.3000;`
D. `const port = process.get("PORT", 3000);`

**20. Co uruchamia poniższe polecenie w terminalu, gdy w projekcie istnieje plik `server.js`?**

```bash
node server.js
```

A. Instaluje zależności projektu
B. Otwiera interaktywną konsolę Node.js (REPL)
C. Uruchamia zawartość pliku `server.js` przez Node.js
D. Tworzy nowy plik `server.js`

---

## Odpowiedzi

1. B
2. C
3. B
4. B
5. B
6. B
7. B
8. B
9. B
10. C
11. B
12. C
13. D
14. A
15. B
16. B
17. C
18. B
19. A
20. C
