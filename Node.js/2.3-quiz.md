# Quiz - Moduły w Node.js i praca z systemem plików

20 pytań jednokrotnego wyboru. Odpowiedzi znajdziesz na końcu pliku - spróbuj najpierw rozwiązać quiz samodzielnie.

**1. Który system modułów jest domyślny w Node.js, jeśli w `package.json` nie ma pola `"type"`?**

A) ES Modules
B) CommonJS
C) AMD
D) UMD

**2. Jak poprawnie wyeksportować dwie funkcje `add` i `subtract` w CommonJS?**

A) `module.exports = { add, subtract };`
B) `export { add, subtract };`
C) `exports.default = { add, subtract };`
D) `return { add, subtract };`

**3. Co się stanie po wywołaniu poniższego kodu, jeśli plik `math.js` znajduje się w tym samym katalogu?**

```javascript
const math = require("math");
```

A) Zaimportuje lokalny plik automatycznie
B) Zwróci `undefined`
C) Zaimportuje plik `math.js` z dowolnego katalogu w projekcie
D) Node.js będzie szukał paczki `math` w `node_modules`, a nie lokalnego pliku

**4. Co trzeba zrobić, żeby w Node.js móc używać składni `import`/`export` w plikach `.js`?**

A) Nic, działa to od razu
B) Zainstalować pakiet `esm-loader`
C) Dodać w `package.json` `"type": "module"` albo użyć rozszerzenia `.mjs`
D) Zmienić rozszerzenie na `.jsx`

**5. Który zapis importu jest poprawny w ES Modules?**

A) `import { add } from "./math"`
B) `import { add } from "./math.js"`
C) `require("./math.js")`
D) `import add "./math.js"`

**6. Do czego służy eksport domyślny (`export default`)?**

A) Do eksportowania jednej głównej wartości z pliku, importowanej bez nawiasów klamrowych
B) Do eksportowania wielu wartości jednocześnie
C) Do eksportowania tylko stałych, nigdy funkcji
D) Do eksportowania modułów wbudowanych Node.js

**7. Do czego służy moduł `path`?**

A) Do odczytu i zapisu plików
B) Do tworzenia serwera HTTP
C) Do zarządzania zależnościami projektu
D) Do budowania i przetwarzania ścieżek do plików w sposób niezależny od systemu operacyjnego

**8. Co zwróci `path.extname("/dane/plik.txt")`?**

A) `"/dane/plik.txt"`
B) `"plik.txt"`
C) `".txt"`
D) `"/dane"`

**9. Co zwróci `path.basename("/dane/plik.txt")`?**

A) `"/dane"`
B) `"plik.txt"`
C) `".txt"`
D) `"/dane/plik.txt"`

**10. Dlaczego zaleca się `path.join()` zamiast ręcznego sklejania ścieżek znakiem `/`?**

A) Bo działa poprawnie niezależnie od systemu operacyjnego (Windows vs Linux/macOS)
B) Bo `path.join()` jest szybszy obliczeniowo
C) Bo `path.join()` sprawdza, czy plik istnieje
D) Bo ręczne sklejanie ścieżek jest niedozwolone w Node.js

**11. Czym różni się `fs.readFileSync` od `fs.readFile`?**

A) Niczym, to aliasy tej samej funkcji
B) `readFileSync` działa tylko na plikach tekstowych, `readFile` na wszystkich
C) `readFile` jest szybszy, bo nie czyta całego pliku
D) `readFileSync` blokuje wykonanie programu do zakończenia odczytu, `readFile` działa asynchronicznie przez callback

**12. Co się wypisze w konsoli po uruchomieniu poniższego kodu?**

```javascript
const fs = require("fs");

fs.readFile("dane.txt", "utf8", (err, content) => {
    console.log("Zawartość pliku");
});

console.log("Koniec skryptu");
```

A) Najpierw "Zawartość pliku", potem "Koniec skryptu"
B) Kolejność jest zawsze losowa i nieprzewidywalna
C) Najpierw "Koniec skryptu", potem "Zawartość pliku"
D) Tylko "Koniec skryptu" się wypisze

**13. Co zwróci `fs.readFileSync("dane.txt")` bez podanego drugiego argumentu?**

A) Zwykły tekst (string)
B) `Buffer` z surowymi danymi binarnymi
C) `null`
D) Błąd - drugi argument jest wymagany

**14. Jak sprawić, żeby `fs.readFileSync` zwrócił gotowy tekst zamiast `Buffer`?**

A) `fs.readFileSync("dane.txt", "utf8")`
B) `fs.readFileSync("dane.txt").toBuffer()`
C) `fs.readFileSync("dane.txt", true)`
D) Nie da się, zawsze zwraca `Buffer`

**15. Co robi `fs.existsSync(sciezka)`?**

A) Tworzy plik lub katalog, jeśli nie istnieje
B) Usuwa plik pod podaną ścieżką
C) Zwraca zawartość pliku jako tekst
D) Sprawdza, czy dany plik lub katalog istnieje

**16. Który moduł `fs` pozwala pisać kod odczytu plików z użyciem `async/await` bez ręcznego opakowywania w Promise?**

A) `fs/sync`
B) `fs/async`
C) `fs/promises`
D) `fs/await`

**17. Jaki jest główny powód, dla którego używa się strumieni (`streams`) zamiast `fs.readFile` przy bardzo dużych plikach?**

A) Strumienie są zawsze szybsze niż zwykły odczyt
B) Strumienie pozwalają przetwarzać dane kawałkami, bez wczytywania całego pliku do pamięci naraz
C) Strumienie automatycznie kompresują dane
D) `fs.readFile` nie działa na plikach większych niż 1 MB

**18. Co robi metoda `pipe()` wywołana na strumieniu odczytu?**

A) Przekazuje dane odczytywane ze strumienia wejściowego bezpośrednio do strumienia wyjściowego
B) Zamyka strumień
C) Konwertuje strumień na `Buffer`
D) Liczy rozmiar pliku

**19. Czym jest `Buffer` w Node.js?**

A) Tymczasowym plikiem na dysku
B) Rodzajem zmiennej środowiskowej
C) Wewnętrzną nazwą modułu `fs`
D) Sposobem przechowywania surowych danych binarnych (sekwencji bajtów)

**20. Który z poniższych zapisów jest zgodny z dobrą praktyką korzystania z modułów w Node.js?**

A) `const fs = require("fs"); import path from "path";` - działa bez problemu w każdym projekcie
B) `import fs from require("fs");` - to poprawny sposób łączenia obu systemów
C) Projekt powinien konsekwentnie używać jednego systemu modułów (albo `require`, albo `import`) w całym projekcie
D) `module.exports = import("path");` - to standardowy sposób łączenia obu systemów

---

## Odpowiedzi

1. B
2. A
3. D
4. C
5. B
6. A
7. D
8. C
9. B
10. A
11. D
12. C
13. B
14. A
15. D
16. C
17. B
18. A
19. D
20. C
