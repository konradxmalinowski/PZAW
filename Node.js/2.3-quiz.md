# Quiz - Moduły, pliki i katalogi w Node.js

31 pytań jednokrotnego wyboru. Odpowiedzi znajdziesz na końcu pliku - spróbuj najpierw rozwiązać quiz samodzielnie.

**1. Który system modułów jest domyślny w Node.js, jeśli w `package.json` nie ma pola `"type"`?**

A) ES Modules
B) CommonJS
C) AMD
D) UMD

**2. Jak poprawnie wyeksportować dwie funkcje `add` i `subtract` w CommonJS?**

A) `module.exports = { add, subtract };`
B) `export { add, subtract };`
C) `exports.default = { add, subtract };`
D) `return { add, subtract };`

**3. Co się stanie po wywołaniu poniższego kodu, jeśli plik `math.js` znajduje się w tym samym katalogu?**

```javascript
const math = require("math");
```

A) Node.js automatycznie zaimportuje lokalny plik
B) Kod zawsze zwróci `undefined`
C) Node.js zaimportuje plik `math.js` z dowolnego katalogu w projekcie
D) Node.js będzie szukał paczki `math` w `node_modules`, a nie lokalnego pliku

**4. Co trzeba zrobić, żeby w Node.js móc używać składni `import`/`export` w plikach `.js`?**

A) Nic, działa to od razu w każdym projekcie
B) Zainstalować pakiet `esm-loader`
C) Dodać w `package.json` `"type": "module"` albo użyć rozszerzenia `.mjs`
D) Zmienić rozszerzenie na `.jsx`

**5. Który zapis importu lokalnego pliku jest poprawny w ES Modules?**

A) `import { add } from "./math"`
B) `import { add } from "./math.js"`
C) `require("./math.js")`
D) `import add "./math.js"`

**6. Do czego służy eksport domyślny (`export default`)?**

A) Do eksportowania jednej głównej wartości z pliku, importowanej bez nawiasów klamrowych
B) Do eksportowania wielu wartości jednocześnie wyłącznie jako obiektu
C) Do eksportowania tylko stałych, nigdy funkcji
D) Do eksportowania modułów wbudowanych Node.js

**7. Do czego służy moduł `path`?**

A) Do odczytu i zapisu plików
B) Do tworzenia serwera HTTP
C) Do zarządzania zależnościami projektu
D) Do budowania i przetwarzania ścieżek do plików w sposób niezależny od systemu operacyjnego

**8. Co zwróci `path.extname("/dane/plik.txt")`?**

A) `"/dane/plik.txt"`
B) `"plik.txt"`
C) `".txt"`
D) `"/dane"`

**9. Co zwróci `path.basename("/dane/plik.txt")`?**

A) `"/dane"`
B) `"plik.txt"`
C) `".txt"`
D) `"/dane/plik.txt"`

**10. Dlaczego zaleca się `path.join()` zamiast ręcznego sklejania ścieżek znakiem `/`?**

A) Bo dobiera poprawne separatory ścieżek dla danego systemu operacyjnego
B) Bo `path.join()` tworzy plik na dysku
C) Bo `path.join()` sprawdza, czy plik istnieje
D) Bo ręczne sklejanie ścieżek jest składniowo zakazane w Node.js

**11. Co oznacza `__dirname` w pliku CommonJS?**

A) Katalog, z którego uruchomiono terminal
B) Katalog, w którym znajduje się bieżący plik `.js`
C) Nazwę aktualnie zalogowanego użytkownika
D) Zawsze katalog główny dysku

**12. Do czego przydaje się `path.parse(...)`?**

A) Do odczytania zawartości pliku tekstowego
B) Do utworzenia katalogu razem z podkatalogami
C) Do rozbicia ścieżki na części, np. `dir`, `base`, `ext`, `name`
D) Do zamiany pliku tekstowego na JSON

**13. Które style pracy z `fs` omawia ta lekcja?**

A) Tylko synchroniczny
B) Tylko synchroniczny i callbackowy
C) Synchroniczny, callbackowy oraz oparty na Promise (`fs.promises`)
D) Tylko `import` i `export`

**14. Czym różni się `fs.readFileSync` od `fs.readFile`?**

A) Niczym, to aliasy tej samej funkcji
B) `readFileSync` działa tylko na plikach tekstowych, a `readFile` tylko na binarnych
C) `readFile` nie czyta całego pliku
D) `readFileSync` blokuje wykonanie programu do zakończenia odczytu, a `readFile` działa asynchronicznie

**15. Co robi `fs.writeFile` albo `fs.writeFileSync`, jeśli plik już istnieje?**

A) Zawsze dopisuje dane na końcu pliku
B) Zawsze rzuca błąd
C) Nadpisuje poprzednią zawartość pliku
D) Zmienia tylko nazwę pliku

**16. Która metoda służy do dopisania danych na końcu pliku bez kasowania poprzedniej zawartości?**

A) `appendFile`
B) `writeFile`
C) `rename`
D) `stat`

**17. Co zwróci `fs.readFileSync("dane.txt")` bez podanego kodowania?**

A) Zwykły tekst (`string`)
B) `Buffer` z surowymi danymi binarnymi
C) `null`
D) Błąd, bo drugi argument jest wymagany

**18. Co jest zwykle pierwszym argumentem callbacka w metodach `fs`, np. w `fs.readFile`?**

A) Błąd operacji, często nazywany `error` albo `err`
B) Zawartość pliku
C) Ścieżka do katalogu projektu
D) Kodowanie tekstu, np. `"utf8"`

**19. Co oznacza kod błędu `ENOENT` przy operacjach na plikach?**

A) Plik jest za duży
B) Plik ma złe kodowanie
C) Operacja zakończyła się sukcesem
D) Plik lub katalog nie istnieje

**20. Po co przy `fs.mkdir` używa się opcji `{ recursive: true }`?**

A) Żeby katalog był ukryty
B) Żeby utworzyć brakujące katalogi po drodze i nie traktować istniejącego katalogu jako błędu
C) Żeby automatycznie usunąć wszystkie pliki w katalogu
D) Żeby odczytać katalog jako tablicę

**21. Co daje opcja `{ withFileTypes: true }` w `fs.readdir`?**

A) Zwraca zawartość każdego pliku
B) Sortuje pliki od największego do najmniejszego
C) Zwraca obiekty `Dirent`, dzięki którym można sprawdzić, czy wpis jest plikiem lub katalogiem
D) Odczytuje katalog rekurencyjnie w każdej wersji Node.js

**22. Które metody obiektu `Dirent` pomagają odróżnić plik od katalogu?**

A) `isFile()` i `isDirectory()`
B) `read()` i `write()`
C) `parse()` i `format()`
D) `open()` i `close()`

**23. Do czego służy `fs.stat`?**

A) Do instalowania paczek npm
B) Do zamiany JSON-a na tekst
C) Do importowania lokalnych modułów
D) Do pobierania informacji o pliku lub katalogu, np. rozmiaru i daty modyfikacji

**24. Która metoda kopiuje jeden plik do drugiego?**

A) `rename`
B) `copyFile`
C) `unlink`
D) `basename`

**25. Co robi `fs.rename`?**

A) Usuwa plik
B) Odczytuje plik jako tekst
C) Zmienia nazwę lub przenosi plik
D) Tworzy pusty katalog

**26. Która metoda usuwa plik?**

A) `unlink`
B) `rmdir`
C) `mkdir`
D) `parse`

**27. Jak usunąć katalog razem z zawartością w wersji synchronicznej?**

A) `fs.unlinkSync("data", { recursive: true })`
B) `fs.rmSync("data", { recursive: true, force: true })`
C) `fs.writeFileSync("data", "")`
D) `path.remove("data")`

**28. Jaki jest główny powód używania strumieni (`streams`) przy bardzo dużych plikach?**

A) Strumienie automatycznie szyfrują dane
B) Strumienie pozwalają przetwarzać dane kawałkami, bez wczytywania całego pliku do pamięci naraz
C) `fs.readFile` nie działa na plikach większych niż 1 MB
D) Strumienie działają tylko w przeglądarce

**29. Co robi metoda `pipe()` wywołana na strumieniu odczytu?**

A) Przekazuje dane ze strumienia odczytu do strumienia zapisu
B) Zamyka proces Node.js
C) Konwertuje strumień na `Buffer`
D) Liczy liczbę znaków w pliku

**30. Czym jest `Buffer` w Node.js?**

A) Tymczasowym katalogiem na dysku
B) Rodzajem zmiennej środowiskowej
C) Wewnętrzną nazwą modułu `fs`
D) Sposobem przechowywania surowych danych binarnych, czyli bajtów

**31. Co daje `require("fs").promises` w porównaniu do zwykłego `fs.writeFile`?**

A) Metody, które zwracają `Promise` i można ich używać z `await`
B) Metody, które zawsze działają synchronicznie
C) Nic - to dokładnie ten sam zapis co `fs.writeFile`
D) Metody dostępne wyłącznie w ES Modules

---

## Odpowiedzi

1. B
2. A
3. D
4. C
5. B
6. A
7. D
8. C
9. B
10. A
11. B
12. C
13. C
14. D
15. C
16. A
17. B
18. A
19. D
20. B
21. C
22. A
23. D
24. B
25. C
26. A
27. B
28. B
29. A
30. D
31. A
