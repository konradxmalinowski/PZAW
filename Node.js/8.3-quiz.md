# Quiz - Projektowanie REST API - operacje CRUD i endpointy do uploadu plików

Quiz oparty wyłącznie na `8-rest-api-crud-upload.md`. 21 pytań jednokrotnego wyboru. Odpowiedzi znajdziesz na końcu pliku - spróbuj najpierw rozwiązać quiz samodzielnie.

**1. Który adres i metoda zgadzają się z konwencją REST dla pobrania listy produktów?**

A. GET /api/getProducts
B. POST /api/products/list
C. GET /api/products
D. GET /api/product

**2. Czym jest "zasób" (resource) w kontekście REST?**

A. Plikiem konfiguracyjnym serwera
B. Rzeczą, którą aplikacja udostępnia przez API, np. użytkownicy albo produkty
C. Nazwą bazy danych
D. Funkcją middleware

**3. Jakiej metodzie HTTP odpowiada operacja "Create" z CRUD?**

A. GET
B. POST
C. PUT
D. DELETE

**4. Jakiej metodzie HTTP odpowiada operacja "Update" z CRUD?**

A. GET
B. POST
C. PUT lub PATCH
D. DELETE

**5. Co oznacza skrót CRUD?**

A. Create, Read, Update, Delete
B. Client, Request, User, Data
C. Create, Route, Use, Deploy
D. Connect, Read, Upload, Download

**6. Za co odpowiada plik w katalogu `routes/`?**

A. Za wykonywanie zapytań SQL
B. Za mapowanie adresu i metody HTTP na odpowiednią funkcję kontrolera
C. Za walidację danych z formularza HTML
D. Za konfigurację połączenia z bazą danych

**7. Za co odpowiada funkcja w katalogu `controllers/`?**

A. Za definiowanie struktury tabeli w bazie danych
B. Za obsługę `req`/`res` i wywołanie odpowiedniej funkcji modelu
C. Za renderowanie widoku EJS
D. Za tworzenie połączenia z bazą danych

**8. Za co odpowiada funkcja w katalogu `models/`?**

A. Za wysyłanie odpowiedzi HTTP do klienta
B. Za sprawdzanie nagłówków żądania
C. Za komunikację z bazą danych - zapytania SQL, bez wiedzy o `req`/`res`
D. Za routing żądań

**9. Jaka jest główna korzyść z podziału kodu na routes/controllers/models?**

A. Aplikacja działa szybciej dzięki temu podziałowi
B. Kod zajmuje mniej miejsca na dysku
C. Każda warstwa ma jedną odpowiedzialność, co ułatwia orientację i utrzymanie kodu
D. Express wymaga takiego podziału, inaczej nie uruchomi serwera

**10. Jaki kod statusu powinien zwrócić endpoint `GET /api/books/:id`, gdy książka o podanym id nie istnieje?**

A. 200
B. 400
C. 404
D. 500

**11. Jaki kod statusu pasuje do sytuacji, gdy klient w `POST /api/books` nie podał wymaganego pola `title`?**

A. 201
B. 400
C. 404
D. 500

**12. Kiedy endpoint powinien zwrócić kod 500?**

A. Gdy klient podał złe dane wejściowe
B. Gdy zasób o podanym id nie istnieje
C. Gdy coś poszło nie tak po stronie serwera, niezależnie od danych klienta
D. Zawsze przy operacji DELETE

**13. Jaki kod statusu typowo zwraca udane `DELETE /api/books/5`, bez treści w odpowiedzi?**

A. 200
B. 201
C. 204
D. 404

**14. W kontrolerze aktualizacji sprawdzane jest `if (affectedRows === 0)`. Co to oznacza?**

A. Zapytanie SQL miało błąd składni
B. Żaden wiersz nie pasował do podanego `id`, więc nic nie zostało zaktualizowane
C. Baza danych jest niedostępna
D. Wszystkie wiersze tabeli zostały zaktualizowane

**15. Do czego służy biblioteka `multer` w Express?**

A. Do wysyłania zapytań SQL do bazy danych
B. Do renderowania widoków EJS
C. Do odbierania plików przesyłanych jako `multipart/form-data`
D. Do generowania tokenów JWT

**16. Co znajduje się w `req.file` po zadziałaniu `upload.single("cover")`?**

A. Treść całego żądania HTTP
B. Informacje o przesłanym pliku (nazwa, rozmiar, ścieżka zapisu)
C. Lista wszystkich plików w katalogu `uploads/`
D. Konfiguracja multera

**17. Co się stanie, jeśli formularz wyśle plik pod nazwą pola `image`, a endpoint ma skonfigurowane `upload.single("cover")`?**

A. Plik zostanie zapisany pod nazwą `cover`
B. `req.file` będzie `undefined`
C. Serwer zwróci błąd 500 automatycznie
D. Multer sam dopasuje nazwę pola

**18. Po co używa się Postmana albo Insomni przy pracy nad API?**

A. Żeby napisać frontend bez Reacta
B. Żeby wysyłać dowolne żądania HTTP (w tym POST/PUT/DELETE z danymi) i sprawdzać odpowiedzi bez pisania frontendu
C. Żeby automatycznie generować dokumentację bazy danych
D. Żeby zarządzać kontem na GitHubie

**19. Dlaczego trzymanie zapytań SQL bezpośrednio w pliku `routes/` jest niedobrą praktyką przy większym projekcie?**

A. Bo Express techniczne tego zabrania
B. Bo miesza odpowiedzialności warstw i utrudnia orientację w kodzie wraz ze wzrostem projektu
C. Bo zapytania SQL w routerze działają wolniej
D. Bo powoduje błędy składni w JavaScript

**20. Który fragment poprawnie rozdziela odpowiedzialności zgodnie z architekturą z tej lekcji?**

A.
```javascript
router.get("/", async (req, res) => {
    const [rows] = await pool.query("SELECT * FROM books");
    res.json(rows);
});
```

B.
```javascript
router.get("/", booksController.getBooks);
```

C.
```javascript
app.use((req, res) => {
    const [rows] = pool.query("SELECT * FROM books");
    res.json(rows);
});
```

D.
```javascript
function getBooks(req, res) {
    return "SELECT * FROM books";
}
```

**21. W kontrolerze `getUser` pierwsza linia w środku funkcji to `return res.status(404).json(...)`. Po co jest tam słowo `return`, skoro Express i tak nie używa wartości zwracanej przez funkcję kontrolera?**

A. Żeby przerwać dalsze wykonywanie funkcji i nie wysłać przypadkiem drugiej odpowiedzi (np. `res.json(user)`) na to samo żądanie
B. Żeby zwrócić kod błędu do bazy danych
C. Bez `return` kod w ogóle by się nie skompilował
D. Żeby middleware mogło przechwycić wynik funkcji

## Odpowiedzi

1. C
2. B
3. B
4. C
5. A
6. B
7. B
8. C
9. C
10. C
11. B
12. C
13. C
14. B
15. C
16. B
17. B
18. B
19. B
20. B
21. A
