# Quiz - Integracja Node.js z bazą danych MySQL/MariaDB

Quiz oparty wyłącznie na `7-mysql-integracja.md`. 20 pytań jednokrotnego wyboru. Odpowiedzi znajdziesz na końcu pliku - spróbuj najpierw rozwiązać quiz samodzielnie.

**1. Co robi `mysql.createPool()`?**

A. Tworzy zestaw wielokrotnie wykorzystywanych połączeń z bazą danych
B. Tworzy pojedyncze połączenie z bazą danych
C. Tworzy nową bazę danych
D. Tworzy tabelę w bazie danych

**2. Dlaczego w aplikacji Express lepiej używać puli połączeń niż pojedynczego `createConnection` przy każdym żądaniu?**

A. Bo pula jest tańsza w utrzymaniu na dysku
B. Bo tworzenie połączenia za każdym razem jest wolniejsze niż ponowne użycie gotowego
C. Bo pojedyncze połączenie nie obsługuje zapytań SELECT
D. Bo pula automatycznie tworzy tabele

**3. Co oznacza skrót DDL w kontekście SQL?**

A. Data Delete Language
B. Database Driver Layer
C. Data Definition Language - definiowanie struktury bazy
D. Data Duplication Logic

**4. Które z poniższych zapytań jest przykładem DML?**

A. CREATE TABLE
B. CREATE INDEX
C. DROP TABLE
D. SELECT * FROM users

**5. Co zwraca `pool.query("SELECT * FROM users")` po `await`?**

A. Tablicę dwuelementową - wiersze i metadane kolumn
B. Sam wynik jako tablicę wierszy
C. Obiekt Promise, który trzeba jeszcze raz odpytać
D. Liczbę zmienionych wierszy

**6. Dlaczego przy odbiorze wyniku SELECT stosuje się `const [rows] = await pool.query(...)`?**

A. Bo `pool.query` zawsze zwraca dokładnie jeden wiersz
B. Bo trzeba wyodrębnić same wiersze z tablicy dwuelementowej zwróconej przez zapytanie
C. Bo `rows` to nazwa zarezerwowana w mysql2
D. Bo inaczej zapytanie się nie wykona

**7. Co to jest przygotowane zapytanie (prepared statement)?**

A. Zapytanie zapisane wcześniej w pliku .sql
B. Zapytanie wykonywane tylko raz przy starcie aplikacji
C. Zapytanie z placeholderami (`?`), do których biblioteka bezpiecznie wstawia wartości
D. Zapytanie z wynikiem zapisanym w cache

**8. Dlaczego sklejanie zapytania SQL ze zmiennych przez template string jest niebezpieczne?**

A. Bo JavaScript nie obsługuje template stringów w zapytaniach
B. Bo zapytanie wtedy działa wolniej
C. Bo mysql2 nie akceptuje template stringów
D. Bo umożliwia atak SQL injection

**9. Co robi `result.insertId` po wykonaniu zapytania INSERT?**

A. Zwraca id ostatnio dodanego wiersza (przy AUTO_INCREMENT)
B. Zwraca liczbę wstawionych wierszy
C. Zwraca cały nowo dodany wiersz jako obiekt
D. Zwraca id połączenia z bazą

**10. Co robi `result.affectedRows` po wykonaniu UPDATE lub DELETE?**

A. Zwraca id zmienionego wiersza
B. Zwraca liczbę wierszy, które zostały zmienione albo usunięte
C. Zwraca treść zmienionych wierszy
D. Zawsze zwraca 1

**11. Co się stanie, jeśli w zapytaniu `UPDATE users SET name = ?` zabraknie warunku `WHERE id = ?`?**

A. Zapytanie zwróci błąd składni
B. Zapytanie nic nie zmieni
C. Zmiana zostanie zastosowana do wszystkich wierszy tabeli
D. Baza sama doda warunek WHERE id = 1

**12. Czym jest klucz obcy (FOREIGN KEY)?**

A. Hasłem do bazy danych
B. Automatycznie generowanym identyfikatorem wiersza
C. Indeksem przyspieszającym zapytania SELECT
D. Kolumną wskazującą na wiersz w innej tabeli, wymuszającą spójność danych

**13. W zapytaniu `SELECT posts.title, users.name FROM posts JOIN users ON posts.user_id = users.id` - co robi JOIN?**

A. Łączy wiersze z dwóch tabel na podstawie podanego warunku
B. Sortuje wyniki alfabetycznie
C. Tworzy nową tabelę tymczasową na stałe
D. Usuwa duplikaty z wyniku

**14. Do czego służy indeks (np. `CREATE INDEX idx_posts_user_id ON posts (user_id)`)?**

A. Do automatycznego usuwania starych wierszy
B. Do przyspieszenia wyszukiwania wierszy po wartości danej kolumny
C. Do szyfrowania danych w kolumnie
D. Do walidacji danych przed zapisem

**15. Jaki jest efekt uboczny dodawania zbyt wielu indeksów do tabeli?**

A. Tabela przestaje działać
B. Odczyty stają się wolniejsze
C. Zapisy (INSERT/UPDATE) stają się wolniejsze, bo indeksy trzeba aktualizować
D. Baza automatycznie usuwa nadmiarowe indeksy

**16. Dlaczego funkcja obsługująca trasę Express, która wykonuje zapytanie do bazy, powinna być oznaczona jako `async`?**

A. Bo Express tego wymaga dla każdej trasy
B. Bo inaczej `pool.query` nie zadziała w ogóle
C. Żeby przyspieszyć wykonanie zapytania
D. Żeby można było użyć `await` przy czekaniu na wynik zapytania

**17. Co się stanie z poniższym kodem?**

```javascript
const rows = pool.query("SELECT * FROM users");
console.log(rows);
```

A. W konsoli pojawi się obiekt Promise, bo zabrakło `await`
B. W konsoli pojawi się tablica wierszy z bazy
C. Kod zwróci błąd składni
D. Zapytanie się nie wykona

**18. Dlaczego zapytania modyfikujące dane (INSERT/UPDATE/DELETE) do bazy powinny być opakowane w `try/catch` w kontekście endpointu Express?**

A. Żeby kod był krótszy
B. Żeby przy błędzie zapytania serwer mógł odesłać sensowną odpowiedź zamiast zawiesić żądanie
C. Bo `try/catch` przyspiesza zapytania SQL
D. Bo mysql2 tego wymaga składniowo

**19. Które stwierdzenie o typach kolumn jest prawdziwe?**

A. Wszystkie dane w MySQL najlepiej trzymać jako VARCHAR, bo jest najbardziej uniwersalny
B. Typ kolumny nie ma znaczenia dla działania zapytań
C. Dobór typu kolumny (INT, DECIMAL, TIMESTAMP itd.) zgodnego z rodzajem danych ułatwia sortowanie i obliczenia
D. Ceny produktów zawsze powinny być typu INT

**20. Który fragment kodu poprawnie i bezpiecznie dodaje nowego użytkownika do bazy?**

A.
```javascript
pool.query("INSERT INTO users (name) VALUES (?)", [name]);
```

B.
```javascript
await pool.query(`INSERT INTO users (name) VALUES ('${name}')`);
```

C.
```javascript
await pool.query("INSERT INTO users (name) VALUES (name)");
```

D.
```javascript
await pool.query("INSERT INTO users (name) VALUES (?)", [name]);
```

## Odpowiedzi

1. A
2. B
3. C
4. D
5. A
6. B
7. C
8. D
9. A
10. B
11. C
12. D
13. A
14. B
15. C
16. D
17. A
18. B
19. C
20. D
